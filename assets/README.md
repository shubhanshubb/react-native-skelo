# Media assets

Demo recordings and screenshots used in the main README.

Expected files (record from the example app):

- `demo.gif` — full example screen loading (profile + list + cards + gallery)
- `grid.gif` — image-grid skeleton loading
- `shimmer.gif` — close-up of the shimmer animation

Keep gifs small (≤ ~5 MB) so the README stays light. Suggested: 600–800px wide,
~10–15 fps.
