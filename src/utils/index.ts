export { resolveStyle, extractDimensions, isPercentage, percentageToNumber } from './styleUtils';

export {
  getComponentName,
  isFragment,
  flattenChildren,
  isTouchableComponent,
  randomWidth,
} from './componentUtils';
